// Rename this file's usage: copy this as config.js and add your real key.
// config.js is gitignored, so your real key never gets pushed to GitHub.

const CONFIG = {
  API_KEY: "YOUR_AVIATIONSTACK_API_KEY_HERE",
  API_BASE_URL: "https://api.aviationstack.com/v1/flights"
};
